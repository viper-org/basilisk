func print(val: i32) -> void;

func strlen(str: i8*) -> i32 {
    let sz = 0;
    while (str[sz] != 0)
        sz = sz + 1;
    return sz;
}

func main(argc: i32, argv: i8**) -> i32 {
    print(strlen(argv[1]));
    return 0;
}