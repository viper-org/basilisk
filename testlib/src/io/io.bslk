module io;

func putchar(c: char) -> void;

func print(s: str) -> void {
    for (let i = 0; i < len(s); i = i + 1) {
        putchar(s[i]);
    }
    return;
}

func printn(s: str) -> void {
    for (let i = 0; i < len(s); i = i + 1) {
        putchar(s[i]);
    }
    putchar(10);
    return;
}