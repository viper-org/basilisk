module test;
import io;

func test(x: str*) -> void {
    printn(*x);
}

func main() -> i32 {
    let x: str = "hello";
    let p = &x;
    x = "world";
    let y: str = x[1:3];
    test(&y);
    test(p);
    let length = len(*p);
    length -= 1;
    return length;
}